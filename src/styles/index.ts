interface Fonts {
    poppins: { [key: string]: string };
}

export enum Colors {
    white = '#FFFFFF',
    black = '#000000',
}

export const fonts: Fonts = {
    poppins: {
        medium: 'Poppins-Medium',
    },
};
